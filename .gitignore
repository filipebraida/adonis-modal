node_modules
coverage
.DS_STORE
.idea
.vscode/
*.log
build
dist
package-lock.json
*.tgz

# Local example app (not committed)
playground

# Playwright MCP artifacts and local screenshots
.playwright-mcp
*.png

# Internal docs (kept locally for dev reference, not published)
docs
